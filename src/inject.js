// `_inject` : a collection's function
// ------------------------------------

import {createReduce} from './_internal';

// **Reduce** builds up a single result from a list of values.
var _inject = createReduce(1);

export {_inject as default};