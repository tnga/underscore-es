// `_restArgs` : (ahem) a function's function
// -------------------------------------------

// This accumulates the arguments passed into an array, after a given index.
export {restArgs as default} from './_internal';